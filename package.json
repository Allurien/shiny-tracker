{
  "name": "shiny-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy --stage production",
    "deploy:dev": "sst deploy --stage dev",
    "remove": "sst remove",
    "secret:set": "sst secret set",
    "secret:list": "sst secret list",
    "web:export": "cd apps/shinybook && npx expo export --platform web --output-dir dist"
  },
  "devDependencies": {
    "sst": "3.19.3",
    "typescript": "^5.5.0"
  }
}
